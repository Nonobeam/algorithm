#include <iostream>

using namespace std;

int main()
{
    int N;
    long long sum = 0;
    cin >> N;
    int a[100000];
    for ( int i = 0; i < N; i++){
        cin >> a[i];
        if ( i % 2 != 0 && i != 0 ){
            sum += a[i];
        }
    }

    cout << sum;
    return 0;
}
